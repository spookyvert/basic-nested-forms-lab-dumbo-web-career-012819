<ul>
<% @recipes.each do |recipe| %>
  <li><%= link_to recipe.title, recipe %></li>
  <ul>
    <% recipe.ingredients.each do |i| %>
      <li> <%= i.name %> quantity: <%= i.quantity %></li>
    <% end %>
  </ul>
<% end %>
</ul>
